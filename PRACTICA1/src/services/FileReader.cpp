
#include "FileReader.h"
#include "../utils/StringUtils.h"
#include <fstream>
#include <iostream>

std::vector<Estudiante> FileReader::leerEstudiante(const std::string& ruta){
    std::vector<Estudiante> lista;
    std::ifstream archivo(ruta);

    if()
}